<article class="post">
      <div class="title">
        <h2>{{title}}</h2>
        <p>{{content}}</p>
      </div>
      <div class="meta">
        {{user.username}}
        {{created_at}}
        |
        <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
      </div>
</article>